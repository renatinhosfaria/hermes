[Service]
# Durable incident receipts survive machine restarts; runtime dir remains for legacy state.
StateDirectory=hermes-fleet-watch
StateDirectoryMode=0700
Environment=FLEET_WATCH_STATE_DIR=/var/lib/hermes-fleet-watch
UMask=0077
